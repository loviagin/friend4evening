import { User } from "@/models/User"

type Props = {
    user: User | null,
}

export default function SettingsProfile({ user }: Props) {
    return (
        <section>
            
        </section>
    )
}